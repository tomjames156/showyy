import { Inter, Libre_Franklin, Kode_Mono } from 'next/font/google'

export const inter = Inter({ subsets: ['latin']});
export const libre_franklin = Libre_Franklin({ subsets: ['latin']});
export const kode_mono = Kode_Mono({
    subsets: ['latin']
})
